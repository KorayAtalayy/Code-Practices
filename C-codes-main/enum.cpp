#include <stdio.h>
#include <conio.h>
#include <stdlib.h>
#include <time.h>
enum Day {Sunday, Monday, Tuesday, Wednesday, Thursday, Friday, Saturday};

int main(){
	enum Day today;
	today = Wednesday;
	printf("Day %d\n",today+1);
	
	enum flag {const1, const2,constN};
	printf("flag is this %d\n",);
	
	
	return 0;
}
